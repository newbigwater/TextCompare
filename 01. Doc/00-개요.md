---
title: TextCompare 설계서
tags: [textcompare, 설계서, moc]
---

# TextCompare 설계서

C# / .NET Framework 4.8 / WinForms 기반의 텍스트·XML·JSON 비교 프로그램. [WinMerge](https://manual.winmerge.org/en/)를 참고했으나, WinMerge의 핵심 결함(라인 기반 비교로 인한 오정렬)을 구조적으로 해결하는 것을 목표로 설계했다.

## 이 설계서의 구성

- [[01-아키텍처]] — 레이어 구조, 빌더 패턴, 네임스페이스별 책임
- [[02-핵심-비교-알고리즘]] — Myers Diff, ghost-line 정렬(WinMerge 오정렬 문제의 해결책)
- [[03-구조적-비교-XML-JSON]] — 객체 단위 XML/JSON 비교, 자연 키 매칭
- [[04-UI-레이어]] — WinForms Controls/Builder, 편집 모드, 드래그앤드롭
- [[05-데이터-흐름]] — 파일 로드부터 화면 렌더링까지 전체 흐름
- [[06-빌드-및-테스트]] — 빌드 방법, SelfTest 구성
- [[07-외부-라이브러리]] — 사용 중인 외부/프레임워크 라이브러리 설명

## 핵심 설계 목표 (요약)

1. **라인 오정렬 문제 해결**: `A,B,C` vs `A,C`(B 삭제) 같은 입력에서 WinMerge류 라인 비교는 B와 C를 잘못 짝짓는다. → [[02-핵심-비교-알고리즘#ghost-line-정렬|ghost-line 정렬]]로 해결.
2. **XML/JSON 객체 단위 비교**: 실무 XML에서는 `id` 속성이 버전 간 재사용되는 경우가 있어, id 기준 매칭은 서로 무관한 엔티티를 잘못 짝짓는다. → [[03-구조적-비교-XML-JSON#자연-키-전략|자연 키(natural key) 전략]]으로 해결.
3. **레이어드/빌더 아키텍처**: 비교 로직(Core/Alignment/Structure)과 UI(Controls/Builders)를 완전히 분리해, 라인 비교와 구조 비교가 동일한 `AlignedRow` 계약을 통해 같은 렌더링 파이프라인을 공유한다.
4. **실시간 편집 및 재비교**: 편집 모드에서 텍스트를 수정하면 디바운스 후 자동 재비교되고, `Ctrl+S`로 저장한다.

## 원본 소스 위치

`D:\50. Utility\Compare\03. Src\TextCompare\` (솔루션: `TextCompare.sln`, 프로젝트: `TextCompare`, `TextCompare.SelfTest`)
